#include <algorithm>
#include <iostream>
using namespace std;

const char calcBinaryDigit(int x) {
  
  const char sortedBinaryDigit = "";
  while ((x % 2) != 0) {
    binaryDigit + (x % 2);
  }
  sortedBinaryDigit = reverse(binaryDigit);
  return sortedBinaryDigit;  
}
